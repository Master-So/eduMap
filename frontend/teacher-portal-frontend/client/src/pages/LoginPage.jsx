import { ArrowLeft, Eye, EyeOff, LockKeyhole } from 'lucide-react';
import { useState } from 'react';
import { Link, useLocation } from 'wouter';
import { useAuth } from '../context/AuthContext.jsx';
export default function LoginPage() { const [, navigate] = useLocation(); const { login } = useAuth(); const [form, setForm] = useState({ name: '', email: '', password: '' }); const [show, setShow] = useState(false); const [status, setStatus] = useState({ loading: false, error: '' }); const update = (key) => (e) => setForm((current) => ({ ...current, [key]: e.target.value })); const submit = async (e) => { e.preventDefault(); setStatus({ loading: true, error: '' }); try { await login(form); navigate('/dashboard/overview'); } catch (error) { setStatus({ loading: false, error: error?.message || 'Unable to sign in. Please try again.' }); } }; return <div className="auth-page"><Link href="/" className="auth-back"><ArrowLeft size={16} /> Back to home</Link><div className="auth-panel"><div className="auth-aside"><div className="brand-lockup light"><img className="brand-image" src="/manus-storage/teacher-portal-mark_2fa77f54.png" alt="" /><span>Teacher <b>Portal</b></span></div><div><span className="eyebrow">Private teacher workspace</span><h1>Your classroom signal starts here.</h1><p>Sign in to create quizzes, connect students, and read performance with confidence.</p></div><span className="auth-aside-foot">Teacher access only</span></div><div className="auth-form-wrap"><div className="auth-form-heading"><span className="eyebrow">Welcome back</span><h2>Sign in to your portal</h2><p>Use the teacher credentials provided by your institution.</p></div><form className="auth-form" onSubmit={submit}><label>Name<input value={form.name} onChange={update('name')} autoComplete="name" placeholder="Your name" /></label><label>Email<input required type="text" value={form.email} onChange={update('email')} autoComplete="email" placeholder="you@institution.edu" /></label><label>Password<div className="password-field"><input required type={show ? 'text' : 'password'} value={form.password} onChange={update('password')} autoComplete="current-password" placeholder="Enter your password" /><button type="button" onClick={() => setShow((value) => !value)} aria-label={show ? 'Hide password' : 'Show password'}>{show ? <EyeOff size={17} /> : <Eye size={17} />}</button></div></label>{status.error && <div className="form-error"><LockKeyhole size={16} />{status.error}</div>}<button className="button primary wide" disabled={status.loading}>{status.loading ? 'Signing in...' : 'Sign in to teacher portal'}</button></form></div></div></div> }

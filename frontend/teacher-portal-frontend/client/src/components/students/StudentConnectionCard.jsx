import { Copy, KeyRound } from 'lucide-react';
import { useState } from 'react';
import { displayValue } from '../../utils/safeData.jsx';
export default function StudentConnectionCard({ connectionKey }) { const [copied, setCopied] = useState(false); const copy = async () => { if (!connectionKey) return; await navigator.clipboard?.writeText(connectionKey); setCopied(true); setTimeout(() => setCopied(false), 1800); }; return <section className="connection-card"><div className="connection-icon"><KeyRound size={20} /></div><div className="connection-copy"><span className="eyebrow">Teacher connection key</span><h2>Invite students to your workspace</h2><p>Students can use this key from their own account to connect with your teacher profile.</p><div className="key-row"><code>{displayValue(connectionKey, 'No connection key available')}</code><button className="button secondary" onClick={copy} disabled={!connectionKey}><Copy size={15} />{copied ? 'Copied' : 'Copy key'}</button></div></div></section>; }

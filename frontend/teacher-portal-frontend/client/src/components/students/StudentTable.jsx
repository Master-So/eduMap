import { Mail, UserRound } from 'lucide-react';
import EmptyState from '../common/EmptyState.jsx';
import { displayValue, initials } from '../../utils/safeData.jsx';
export default function StudentTable({ students }) { if (!students?.length) return <EmptyState title="No students connected yet." description="Share your teacher connection key to let students join your workspace." />; return <div className="table-wrap"><table><thead><tr><th>Student</th><th>Email</th><th>Joined</th><th>Performance</th></tr></thead><tbody>{students.map((student, index) => <tr key={student?.id || student?._id || index}><td><div className="table-person"><span className="avatar small">{initials(student?.name)}</span><span>{displayValue(student?.name, 'Unnamed student')}</span></div></td><td><span className="muted-cell"><Mail size={14} />{displayValue(student?.email)}</span></td><td>{displayValue(student?.joinedAt || student?.createdAt)}</td><td>{displayValue(student?.averageScore, 'No score yet')}</td></tr>)}</tbody></table></div>; }
